package com.ll.exam;

import org.testng.annotations.Test;

import static org.testng.AssertJUnit.*;



public class AppTest {

/*
    @Test
    public void plusTest01(){
        int a,b; // 1~100 까지 임의의 자연수 생성
        int rs = Calculator.plus((a = (int)(Math.random()*1000)),(b = (int)(Math.random()*1000)));
        assertEquals(a+b,rs);
    }

    @Test
    public void plusTestError() {
        int rs = Calculator.plus(5,2);
        assertEquals(10,rs); // 5+2 = 7 이므로 에러 발생
    }

    @Test
    public void plusTest02(){
        int rs = Calculator.plus(5,2);
        assertSame(7,rs);
    }
    @Test
    public void plusTest03(){

        assertTrue(Calculator.plus(5,2)==7);
    }
    @Test
    public void substractTest01(){
        int a,b; // 1~100 까지 임의의 자연수 생성
        int rs = Calculator.substract((a = (int)(Math.random()*1000)),(b = (int)(Math.random()*1000)));
        assertEquals(a-b,rs);
    }
    @Test
    public void multiply01(){
        int a,b; // 1~100 까지 임의의 자연수 생성
        int rs = Calculator.multiply((a = (int)(Math.random()*1000)),(b = (int)(Math.random()*1000)));
        assertEquals(a*b,rs);
    }
    @Test
    public void divideTest01(){
        int a,b; // 1~100 까지 임의의 자연수 생성
        int rs = Calculator.divide((a = (int)(Math.random()*1000)),(b = (int)(Math.random()*1000)));
        assertEquals(a/b,rs);
    }

 */

}
